# AI-Ops Insights Generator Environment Configuration

# Base URL for the AI-Ops platform (without the API path)
AIOPS_DOMAIN=http://localhost:4047

# Base URL for Aegis API calls (optional, falls back to AIOPS_DOMAIN)
AEGIS_DOMAIN=http://localhost:4047

# Bearer token for authentication (optional)
AIOPS_TOKEN=TOKEN

# Device selection strategy: 'random' or 'sequential' (default: random)
AIOPS_DEVICE_SELECTION=sequential

# Insight picker strategy: 'sequential' or 'random' (default: sequential)
INSIGHT_PICKER=random